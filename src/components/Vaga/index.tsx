import { LinkButton, TituloVaga, VagaStyle } from './style'

type Props = {
  key: number
  titulo: string
  localizacao: string
  nivel: string
  modalidade: string
  salarioMin: number
  salarioMax: number
  requisitos: string[]
}

const Vaga = (props: Props) => (
  <VagaStyle>
    <TituloVaga>{props.titulo}</TituloVaga>
    <ul>
      <li>Localizacao: {props.localizacao}</li>
      <li>Senioridade: {props.nivel}</li>
      <li>Tipo de contratacao: {props.modalidade}</li>
      <li>
        Salário: {props.salarioMin} - {props.salarioMax}
      </li>
      <li>Requisitos: {props.requisitos.join(', ')}</li>
    </ul>
    <LinkButton>Ver detalhes e candidatar-se</LinkButton>
  </VagaStyle>
)

export default Vaga
